# Claude Visual Generator Python Dependencies
# Install: pip install -r requirements.txt

# Claude Agent SDK (uses OAuth from Claude Pro/Max subscription)
claude-agent-sdk>=0.1.0

# Async support
aiofiles>=23.0.0
aiohttp>=3.9.0

# Type hints
typing-extensions>=4.0.0

# Image processing (used by segmentation + background generation)
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# OpenAI (background generation via image inpainting)
openai>=1.0.0

# Person segmentation (RVM — Robust Video Matting)
torch>=2.0.0
torchvision>=0.15.0
