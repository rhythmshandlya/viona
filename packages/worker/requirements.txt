# Claude Visual Generator Python Dependencies
# Install: pip install -r requirements.txt

# Claude Agent SDK (uses OAuth from Claude Pro/Max subscription)
claude-agent-sdk>=0.1.0

# Async support
aiofiles>=23.0.0
aiohttp>=3.9.0

# Type hints
typing-extensions>=4.0.0

# Head tracking (MediaPipe Holistic for face + body detection)
mediapipe>=0.10.0
opencv-python>=4.8.0
numpy>=1.24.0
