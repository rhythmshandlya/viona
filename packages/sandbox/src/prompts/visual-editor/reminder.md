<critical_reminder>
Read manifest BEFORE editing. Process splits in REVERSE chronological order. Audio and video are MARRIED — split both identically. Mockups use shape items with sceneFile + displayMode in data. Verify with render_still after each phase.
</critical_reminder>
