<critical_reminder>
STRICT VOCABULARY ONLY:
- Display modes: Overlay, Stacked, Fullscreen. NEVER "split-screen".
- Transitions: 15 total (3 same-mode + 12 cross-state), all 300ms. Every scene specifies IN and OUT.
- Overlays are polished animations, NOT text labels — but SIMPLER than Stacked/Fullscreen (fewer elements, snappier timing, single focal point).
- Visual concepts must include: metaphor/anchor + primary motion + emotional beat. NOT layout descriptions.
- Animation briefs must sync to transcript — reference specific speaker words as timing anchors for visual events.
- No two adjacent scenes use the same primary motion technique. Vary energy and spatial approach across scenes.
- No captions in the plan (separate system).
- No kinetic typography as standalone.
- NEVER describe SVG stroke animations: "draws itself", "stroke-by-stroke", "hand-drawn line", "outlined shapes". Use solid fills, clip-path reveals, scale-in, animated width/height.
- Entire timeline must be covered — no speaker-only gaps.
- Depth vocabulary (emerge-behind, peek-sides, weave-through, split-depth, etc.) is OVERLAY MODE ONLY. Never use in Stacked or Fullscreen briefs.
- Punch-ins only during overlay segments.
- Every field in the per-scene schema is REQUIRED. No omissions.
- Transcript text must match EXACTLY — no paraphrasing.
- Speaker transitions (Speaker → X, X → Speaker) only at video start/end.
- Mid-video: chain directly between modes (Overlay → Stacked, Stacked → Fullscreen, etc.).
</critical_reminder>
