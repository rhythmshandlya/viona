// V2: layout is in AI-generated Composition.tsx
// This panel was used for manual layout mode switching (PiP/Stacked presets).
// The entire panel is disabled in v2 — layout decisions are made by the AI director.

export function PiPControlPanel() {
  return (
    <div className="p-4 text-sm text-[var(--editor-text-muted)]">
      Layout is now controlled by AI-generated compositions.
    </div>
  );
}
