// Keep in sync with globals.css @theme — only used for HTML <meta> attributes that require raw hex values
export const theme = {
  primary: "#6B3F69",
  secondary: "#8D5F8C",
  accent: "#A376A2",
  soft: "#DDC3C3",
} as const;
