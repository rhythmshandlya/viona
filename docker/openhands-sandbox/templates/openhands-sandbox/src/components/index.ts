/**
 * Clipify Component Library
 *
 * Pre-built animation and visual components for the Visual Generator.
 * Import these components instead of generating common patterns from scratch.
 *
 * @example
 * import { GravityDrop, Counter, ParticleStream, SafeZone } from './components';
 *
 * Categories:
 * - Animation (physics, path, timing)
 * - Visual (particles, shapes, data, effects)
 * - Layout (safe zones, containers)
 * - Utils (easing, colors)
 */

// Animation components
export * from './animation';

// Visual components
export * from './visual';

// Layout components
export * from './layout';

// Utilities
export * from './utils';
