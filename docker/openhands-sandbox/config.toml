# OpenHands LLM Configuration for Visual Generation
# This file configures LLM behavior for the Remotion visual generation agent.
#
# IMPORTANT: Gemini 3.x models require temperature=1.0
# Google docs warn that lower temperatures cause "unexpected behavior, looping issues"

[llm]
# Default model - can be overridden via CLI --model flag
model = "openrouter/xiaomi/mimo-v2-flash"

# Standard temperature for MiMo reasoning model
temperature = 0.7

# Token limits
max_output_tokens = 8192
# max_input_tokens = 1000000  # 1M context for Gemini Flash

# Retry configuration for rate limits
num_retries = 8
retry_min_wait = 5
retry_max_wait = 60

# Cost tracking (USD per 1M tokens)
input_cost_per_1m = 0.10
output_cost_per_1m = 0.40

# Disable vision to reduce token costs (we use screenshots separately)
disable_vision = false

# Enable prompt caching if supported by provider
caching_prompt = true

[llm.mimo-flash]
# MiMo v2 Flash - fast reasoning model from Xiaomi
model = "openrouter/xiaomi/mimo-v2-flash"
temperature = 0.7
max_output_tokens = 8192
input_cost_per_1m = 0.07
output_cost_per_1m = 0.28

[llm.gemini-pro]
# Gemini Pro - for complex tasks requiring higher quality
model = "openrouter/google/gemini-2.5-pro"
temperature = 0.7
max_output_tokens = 8192
input_cost_per_1m = 1.25
output_cost_per_1m = 5.00

[llm.claude]
# Claude 3.5 Sonnet - alternative for comparison
model = "anthropic/claude-3-5-sonnet-20241022"
temperature = 0.3
max_output_tokens = 8192
input_cost_per_1m = 3.00
output_cost_per_1m = 15.00

[llm.claude-proxy]
# Claude via Claude Code proxy (aggressive context management)
# Uses automatic detection based on model name containing 'claude' or 'anthropic'
# Context: 200K (vs Gemini's 1M), so we use aggressive limits
model = "claude-sonnet-4-20250514"
temperature = 0.3
max_output_tokens = 8192
input_cost_per_1m = 3.00
output_cost_per_1m = 15.00

# Claude-specific context management (auto-applied when Claude detected)
# These values are defined in visual_generator.py CLAUDE_CONFIG
# condenser_max_size = 35        # vs 100 for Gemini
# condenser_max_size_eval = 20   # vs 50 for Gemini
# generator_max_iterations = 25  # vs 50 for Gemini
# evaluator_max_iterations = 8   # vs 15 for Gemini
# skills_to_load = ["remotion-best-practices", "file-editing-guide"]

[agent]
# Agent behavior configuration
max_iterations = 3
quality_threshold = 70

# Timeout in seconds
timeout = 900  # 15 minutes

# Enable self-healing TypeScript validation
enable_typescript_validation = true

# Enable visual evaluation with screenshots
enable_visual_evaluation = true

[agent.generator]
# Generator agent - creates the Remotion components
llm_config = "mimo-flash"
enable_prompt_extensions = true

[agent.evaluator]
# Evaluator agent - scores visual quality
llm_config = "mimo-flash"
enable_prompt_extensions = true

[skills]
# Skills (microagents) to load automatically
auto_load = [
    "remotion-best-practices",
    "visual-design",
    "file-editing-guide",
]

# Skill for visual evaluation
evaluator_skills = [
    "scoring-rubric",
]

[remotion]
# Remotion project configuration
fps = 30
default_width = 1080
default_height = 1920

# Bundle output settings
bundle_log_level = "verbose"
bundle_timeout = 300  # 5 minutes

# Render settings
render_timeout = 600  # 10 minutes
